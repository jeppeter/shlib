
ifndef __FILE_OP_MAK__
__FILE_OP_MAK__:=1


define readlink_f
$(shell ${PERL} -e '%PERL_READLINK_STR%' --  $(1) )
endef

define get_basename
$(shell ${PERL} -e '%PERL_BASENAME_STR%' -- $(1) )
endef

define get_exename
$(shell ${PERL} -e '%PERL_EXENAME_STR%' -- $(1) )
endef

define get_cfile
$(shell ${PERL} -e '%PERL_FILTEROP_STR%' -- cfile $(1))
endef

define get_cppfile
$(shell ${PERL} -e '%PERL_FILTEROP_STR%' -- cppfile $(1))
endef

define get_sfile
$(shell ${PERL} -e '%PERL_FILTEROP_STR%' -- sfile $(1))
endef

define get_lcfile
$(shell ${PERL} -e '%PERL_FILTEROP_STR%' -- lcfile $(1))
endef

define get_lcppfile
$(shell ${PERL} -e '%PERL_FILTEROP_STR%' -- lcppfile $(1))
endef

define get_lsfile
$(shell ${PERL} -e '%PERL_FILTEROP_STR%' -- lsfile $(1))
endef

## __FILE_OP_MAK__ ##
endif